local user = require "user"

user.option.g = {
    NERDSpaceDelims = 1, -- Atuo spacing after delimter
    NERDCompactSexyComs = 1, -- Using compat format for multi-line
    -- NERDDefaultAlign = "left",      -- Align line-wise rather than code indent
    NERDCommentEmptyLines = 1, -- Allow commenting and inverting empty lines
    NERDTrimTrailingWhitespace = 1, -- Trimming trailing whitespace
    NERDToggleCheckAllLines = 1, -- Check all selected lines when Toggle
}
