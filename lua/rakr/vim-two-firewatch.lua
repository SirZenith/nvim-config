local user = require "user"

user.o.background = "dark" -- or light if you prefer the light version
user.g = {
    two_firewatch_italics = 1,
    airline_theme = "twofirewatch",
}

user.theme.colorscheme = "two-firewatch"
